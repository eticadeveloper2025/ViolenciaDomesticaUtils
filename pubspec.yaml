name: violencia_domestica_utils
description: Pacote de utilitários para os apps de ajuda do projeto Violencia Domestica
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.7.2

# Add regular dependencies here.
dependencies:
  brasil_fields:
  encrypt: ^5.0.3
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.2.1
  geolocator: ^14.0.0
  get: ^4.7.2
  http: ^1.4.0
  internet_connection_checker_plus: ^2.7.2
  # path: ^1.8.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
